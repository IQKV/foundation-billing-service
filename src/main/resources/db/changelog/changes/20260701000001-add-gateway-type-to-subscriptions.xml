<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="20260701000001" author="iqkv">
        <comment>Add gateway_type column to subscriptions to record which payment gateway
            created/owns this subscription. Defaults to STRIPE for all existing rows.</comment>

        <addColumn tableName="subscriptions">
            <column name="gateway_type" type="VARCHAR(32)" defaultValue="STRIPE">
                <constraints nullable="false"/>
            </column>
        </addColumn>
        <rollback>
            <dropColumn tableName="subscriptions" columnName="gateway_type"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
